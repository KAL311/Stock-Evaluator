# Stock Evaluator — runtime + test dependencies.
#
# Versions are pinned exact (==) on purpose: the golden-master regression
# (tests/test_golden_master.py) requires bit-exact scorer output, and numpy/
# pandas/scipy float behaviour drifts across releases. Per tests/README.md,
# a divergence right after a numpy/pandas upgrade means "pin the dependency,
# do not re-freeze to paper over it" — these pins are that guardrail.
# Pinned to the versions the current golden master (HEAD b0e3988) was frozen on.

# --- Core numeric / data ---
numpy==2.4.2
pandas==2.3.3
scipy==1.17.1

# --- Data sources / HTTP ---
requests==2.32.5
yfinance==1.3.0          # live prices (scripts/refreshprice.py)
simfin==1.0.2            # quarterly/TTM + SimFin rollback path
finvizfinance==1.3.0     # per-ticker quote (src/stock_info.py)

# --- Test ---
pytest==9.0.3            # unit + golden-master suite (tests/)
